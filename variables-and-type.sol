// SPDX-LICENSE-INSTANTITION: MIT

pragma solidity 0.8.0

contract learnVariables {
    // all our code
    bool isOwner = true;
    string errorMessageText = 'Error! This is fuckery';

    // create a new variable called wallet as an integer
    uint wallet;

    // create boolean spend
    bool spend;
    
    // create string with identifier notifySpend
    string notifySpend

    wallet = 50;
    spend = false;
    notifySpend = 'you have spent money';
}